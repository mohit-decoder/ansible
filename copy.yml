- name: This is copy file playbook
  hosts: all
  become: yes
  tasks: 
    - name: fileisbeingcopy
      ansible.builtin.copy:
        src: /home/ubuntu/abhay
        dest: /opt/
        remote_src: yes